"""Tool scripts for the custom_platform scaffold.

After `uv sync`, prefer invoking the project interpreter directly:
    .venv/bin/python tools/<script>.py [args]

If you still want `uv`, prefer:
    uv run --no-sync tools/<script>.py [args]
"""
